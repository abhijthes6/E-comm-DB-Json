{
  "name": "my-ecomm-json-server",
  "version": "1.0.0",
  "description": "Fake REST API for e-commerce app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
